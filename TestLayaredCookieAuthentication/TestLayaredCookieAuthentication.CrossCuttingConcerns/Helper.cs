using System;
using System.Collections.Generic;
using System.Text;

namespace TestLayaredCookieAuthentication.CrossCuttingConcerns
{
    public class Helper
    {
        /*public static string CustomerCodeGenerate(Customer customer)
        {
            //C+yılın son 2 rakamı +bulunduğu ay 2 rakamlı+son müşterinin son Id sini
            return "M" +
                (DateTime.Now.Month > 10 ? DateTime.Now.Month.ToString() : "0" + DateTime.Now.Month) + DateTime.Now.Year.ToString().Substring(2, 2) +
                (customer == null ? 1 : customer.InCustomerId);
        }*/
    }
}
